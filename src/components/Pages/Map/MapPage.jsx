import React from "react";

/*
1. array
2. doc to map over the array
3. doc to export/ render the component on teo the page? (similar to a Card in portfolio project?)
*/

export const MapPage = ({ locationData }) => {
  return (
    <main className="cardQ">
      <header className="main__mapData-header">Locations</header>
    </main>
  );
};

// export default MapPage;
